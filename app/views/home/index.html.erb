<div class="main_head">

 <h1>Lakelandで新たな世界を旅しよう</h1>

 <div>
  <%= form_with(url: search_rooms_path, local: true, method: :get ) do |f| %>
    <%= f.text_field :search_area, placeholder: "エリア名を入力" %>
    <%= f.submit "検索" %>
  <% end %>
 </div>

</div>


<div class="recommendation">
 <h3 class="text_area">おすすめのエリア</h3>

 <div class="recommendation_area">

  <div>
    <%= form_with(url: search_rooms_path, local: true, method: :get ) do |f| %>
     <%= f.hidden_field :search_area, value: "東京" %>
     <%= image_submit_tag('tokyo.jpg', class: "image_area") %>
    <% end %>
  </div>

   <div>
    <%= form_with(url: search_rooms_path, local: true, method: :get ) do |f| %>
     <%= f.hidden_field :search_area, value: "京都" %>
     <%= image_submit_tag('kyoto.jpg', class: "image_area") %>
    <% end %>
   </div>
   
   <div>
    <%= form_with(url: search_rooms_path, local: true, method: :get ) do |f| %>
     <%= f.hidden_field :search_area, value: "大阪" %>
     <%= image_submit_tag('osaka.jpg', class: "image_area") %>
    <% end %>
   </div>
   
   <div>
    <%= form_with(url: search_rooms_path, local: true, method: :get ) do |f| %>
     <%= f.hidden_field :search_area, value: "札幌" %>
     <%= image_submit_tag('sapporo.jpg', class: "image_area") %>
    <% end %>
   </div>  
  
 </div>
</div>
